# Figure prompt: fig-automation-vs-ai

Concept: the core distinction of the chapter. Automation is deterministic (same input, same output, every time). AI is probabilistic (messy input, plausible output, usually right but needs checking).

Prompt for image tool:
"A clean, minimal two-panel comparison diagram, flat vector style, muted professional palette. Left panel labelled 'AUTOMATION': three identical inputs flow through a box marked 'RULE' to three identical outputs, caption 'same in, same out, every time'. Right panel labelled 'AI': three different, mismatched inputs flow through a box marked 'AI' to a single output marked with a question mark, caption 'messy in, plausible out, usually right, check it'. Calm and clear, not cartoonish."

Alt text: A two-panel diagram contrasting deterministic automation (identical inputs and outputs through a rule) with probabilistic AI (varied inputs producing a plausible output that needs checking).
