# Figure prompt: fig-connector

Concept: a no-code connector turns one trigger into a chain of actions. A TRIGGER ("a form is filled in") flows into a CONNECTOR (no code), which fans out to four actions: add to the CRM, send the confirmation, alert the team, create the task. Caption: one event, four jobs, in seconds.

Prompt for image tool:
"A clean, minimal flow diagram, flat vector style, muted professional palette. On the left a box 'TRIGGER: a form is filled in', an arrow to a central emphasised box 'CONNECTOR (no code)', which fans out with arrows to four stacked action boxes on the right: 'add to the CRM', 'send the confirmation', 'alert the team', 'create the task'. Caption beneath: one event, four jobs, in seconds. Calm and clear, not cartoonish."

Alt text: A diagram showing one trigger flowing through a no-code connector that fans out into four automatic actions.
